const config = {
    // 服务器数据库
    dataBase: {
        DATABASE: 'mind-x',
        USERNAME: 'root',
        PASSWORD: '252238Lzy',
        PORT: '3306',
        HOST: 'localhost',
        multipleStatements: true //允许一次执行多条sql
    }
}

module.exports = config